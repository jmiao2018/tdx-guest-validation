# @name USBHOST_STRESS_OTHER_TESTS
# @desc usbhost switch stress with peripheral tests
# @requires usbhost
# @setup_requires

# USBHOST SWITCH FUNC TEST
USBHOST_XS_STRESS_SWITCH_USB_OTHER usb_switch_tests.sh -p printer -q scanner -r headset -s camera -t 100
